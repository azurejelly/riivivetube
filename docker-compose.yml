services:
  riivivetube:
    container_name: riivivetube
    image: azurejelly/riivivetube:latest
    restart: unless-stopped
    build:
        context: .
        dockerfile: Dockerfile
    ports:
      - "5005:5005"
    environment:
      SERVER_ADDR: ${SERVER_ADDR:-}
